<% if object.errors.any? %>
  <div id="error_explanation" class="text-danger">
    <h3><%= pluralize(object.errors.count, "error") %> prohibited this obj from being saved:</h3>
    <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>