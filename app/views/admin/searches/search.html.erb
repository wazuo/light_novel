<div class="container">
  <div class="row">
    <div class="col">
      <% if @model == 'member'%>
        <h3>ユーザの検索結果 "<%= @content %>"の該当一覧 (検索カテゴリー) <%= @search %></h3>
        <%= render 'admin/members/index', members: @records %>
      <% elsif @model == 'manga' %>
        <h3>マンガの検索結果 "<%= @content %>"の該当一覧 (検索カテゴリー）<%= @search%></h3>
        <%= render 'admin/mangas/index', mangas: @records %>
      <% end %>
    </div>
  </div>
</div>
